app = 'houndjob-backend'
primary_region = 'gru'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"     # ← Costo < 15.000 CLP/mes
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1